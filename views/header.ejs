<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>EA Gaming Review Blog</title>
    <style>
        .container {
            display: flex;
            flex-direction: column;
            justify-content: center;
            width: 60%;
            margin: 0 auto;
            text-align: center;
        }
        .post-wrapper {
            display: flex;
            flex-direction: column;
            margin: 12px;
        }
        .post-card {
            display: flex;
            flex-direction: column;
            padding: 24px;
            border: 3px solid #000;
        }
        .post-title {
            margin: 12px;
            font-size: 20px;
            font-weight: 700;
        }
        .post-body {
            padding: 24px;
            font-size: 12px;
        }
        .post-footer, .comment-footer {
            display: flex;
            flex-direction: row;
            justify-content: space-between;
            font-size: 14px;
        }
        .post-date, .post-author, .comment-date, .comment-author {
            text-align: bottom;
            margin: 0;
        }
        .comments {
            padding: 12px;
            border: 3px solid #AAA;
            border-top: none;
        }
        .comment-header {
            border: 3px solid #AAA;
            padding: 12px;
            font-size: 14px;
            font-weight: 700;
        }
    </style>
</head>